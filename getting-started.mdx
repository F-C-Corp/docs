---
title: "Getting started"
description: "Create your API key, onboard a merchant, and complete your first operation"
---

This guide walks you through the smallest useful OpenFiskal integration. You create an API key in the tenant dashboard, onboard a merchant, create its location and register, and then process operations from your backend.

<Info>
  All examples use `https://sandbox.api.openfiskal.com/v1`. Replace with `https://api.openfiskal.com/v1` when you go live.
</Info>

## Prerequisites

- An OpenFiskal tenant
- A tenant-scoped, country-scoped API key created in your tenant dashboard
- `curl` or an HTTP client
- A backend service that holds the API key securely

## Authentication model

Use the standard bearer header on every request:

```bash
-H "Authorization: Bearer ofk_live_abc123..."
```

Every organization-scoped request must also include:

```bash
-H "X-OpenFiskal-Organization: org_01HXYZ"
```

This header value is the organization ID returned by `POST /organizations`, not your API key.

## Step by step

<Steps>
  <Step title="Create your API key">
    Start in the OpenFiskal tenant dashboard. Create a tenant-scoped, country-scoped API key for the country you want to operate in, then store it in your secrets manager.
  </Step>
  <Step title="Create an organization">
    An organization represents the merchant legal entity.

    ```bash
    curl -X POST https://sandbox.api.openfiskal.com/v1/organizations \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "Content-Type: application/json" \
      -d '{
        "legal_name": "Mustermann GmbH",
        "country_code": "DEU",
        "tax_id": "DE123456789",
        "address": {
          "line1": "Friedrichstraße 42",
          "city": "Berlin",
          "postal_code": "10117",
          "country_code": "DEU"
        }
      }'
    ```

    The returned `id` becomes the `X-OpenFiskal-Organization` header on every organization-scoped request that follows.
  </Step>
  <Step title="Create a location">
    A location is a physical point of sale such as a store or restaurant.

    ```bash
    curl -X POST https://sandbox.api.openfiskal.com/v1/locations \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Content-Type: application/json" \
      -d '{
        "name": "Berlin Mitte",
        "address": {
          "line1": "Friedrichstraße 42",
          "city": "Berlin",
          "postal_code": "10117",
          "country_code": "DEU"
        },
        "timezone": "Europe/Berlin"
      }'
    ```
  </Step>
  <Step title="Create a register">
    A register is the logical checkout or terminal that creates operations.

    ```bash
    curl -X POST https://sandbox.api.openfiskal.com/v1/registers \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Content-Type: application/json" \
      -d '{
        "location_id": "loc_01HXYZ",
        "country_code": "DEU",
        "name": "Register 1",
        "external_id": "pos-register-1"
      }'
    ```
  </Step>
  <Step title="Start an operation">
    Start every operation with `POST /operations`. The request includes the source and a typed `data` object. The response returns `op_revision`, which starts at `1`.

    ```bash
    curl -X POST https://sandbox.api.openfiskal.com/v1/operations \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Idempotency-Key: op-order-1001-start" \
      -H "Content-Type: application/json" \
      -d '{
        "location_id": "loc_01HXYZ",
        "register_id": "reg_01HXYZ",
        "source": "POS",
        "data": {
          "type": "sale",
          "external_reference": "order-1001",
          "currency": "EUR",
          "amount": 4250
        }
      }'
    ```
  </Step>
  <Step title="Update the operation while it is open">
    You can patch the operation as long as it is still `open`. `op_revision` is caller-driven. Start returns `1`, so the first mutation must send `2`.

    ```bash
    curl -X PATCH https://sandbox.api.openfiskal.com/v1/operations/op_01HXYZ \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Content-Type: application/json" \
      -d '{
        "op_revision": 2,
        "data": {
          "type": "sale",
          "external_reference": "order-1001",
          "currency": "EUR",
          "amount": 4750,
          "line_items": [
            {
              "name": "Lunch menu",
              "quantity": 1,
              "unit_price": 4750,
              "total_amount": 4750
            }
          ]
        }
      }'
    ```
  </Step>
  <Step title="Complete the operation">
    Completion is modeled as an operation sub-resource and includes the reconciled payment transactions. Because the previous successful mutation used revision `2`, completion now sends `3`.

    ```bash
    curl -X PATCH https://sandbox.api.openfiskal.com/v1/operations/op_01HXYZ/complete \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Idempotency-Key: op-order-1001-complete" \
      -H "Content-Type: application/json" \
      -d '{
        "op_revision": 3,
        "transactions": [
          {
            "transaction_id": "txn_1001_1",
            "payment_type": "card",
            "kind": "sale",
            "amount": 4750,
            "currency": "EUR",
            "status": "success",
            "gateway": "shopify_payments",
            "card_brand": "visa",
            "external_reference": "ch_123",
            "processed_at": "2026-02-26T12:05:00Z"
          }
        ]
      }'
    ```
  </Step>
  <Step title="Cancel an open operation when needed">
    If the customer abandons the checkout before completion, cancel the existing open operation. Because the previous successful mutation used revision `2`, cancellation would now send `3`.

    ```bash
    curl -X PATCH https://sandbox.api.openfiskal.com/v1/operations/op_01HXYZ/cancel \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Idempotency-Key: op-order-1001-cancel" \
      -H "Content-Type: application/json" \
      -d '{
        "op_revision": 3,
        "reason": "customer_abandoned_checkout"
      }'
    ```

    Use cancellation only for operations that are still `open`. Completed operations remain completed.
  </Step>
  <Step title="Read and list operations">
    Use `GET /operations` to list operations and `GET /operations/{id}` to read one operation, including its current `op_revision`.

    ```bash
    curl https://sandbox.api.openfiskal.com/v1/operations \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ"
    ```
  </Step>
  <Step title="Create an export job">
    Exports are asynchronous. First create the job.

    ```bash
    curl -X POST https://sandbox.api.openfiskal.com/v1/exports \
      -H "Authorization: Bearer ofk_live_abc123..." \
      -H "X-OpenFiskal-Organization: org_01HXYZ" \
      -H "Content-Type: application/json" \
      -d '{
        "type": "operations",
        "format": "csv",
        "from": "2026-02-01T00:00:00Z",
        "to": "2026-02-29T23:59:59Z"
      }'
    ```
  </Step>
</Steps>

## Next steps

- Read [POS operations](/pos-operation-ingestion) for the operation lifecycle in more detail
- Read [Hosted Receipts API](/hosted-receipts-api) for receipt retrieval, rendering, and delivery
